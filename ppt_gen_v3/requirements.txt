fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# optional: renders the deck viewer through PowerPoint itself.
# Without it the built-in SVG approximation is used instead.
pywin32>=306; sys_platform == 'win32'
