fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# the deck viewer: previews are pages rasterised out of the library PDF.
# Pure wheels, no system binary, identical on Windows and Linux.
pypdfium2>=4
pillow>=10

# optional, Windows only: PowerPoint automation. Needed by
# tools/make_library_pdf.py and by the exact-render fallback. The deployed app
# never uses it.
pywin32>=306; sys_platform == 'win32'

# optional: the `graph` render backend, which converts the built deck with
# Office Online over Microsoft Graph. Needed only when the four GRAPH_*
# variables are set; without them the backend probes as unavailable and
# nothing imports this.
msal>=1.28
